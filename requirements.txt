# Remote Mouse Controller — dependances Python
# Installation : pip install -r requirements.txt

# Serveur WebSocket (server.py)
websockets>=12.0
pyautogui>=0.9.54
python-dotenv>=1.0.0

# Lanceur + icone system tray (launcher.pyw)
pystray>=0.19.5
pillow>=10.0.0
